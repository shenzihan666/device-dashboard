# `<repo-name>` 学习与落地计划

> 与 `.study-meta.md` 配套使用。Cursor 在执行 `learnfrom` 技能 Phase 5 时
> 会写这份文档；之后每次复参考也会在末尾追加新章节。

## 1. 学习目标

- **焦点（Phase 1.2）**：…
- **借鉴层级（Phase 1.3）**：…（A 只看思路 / B 借鉴模式 / C 改写片段 /
  D 直接复用代码）
- **本项目期望的产出**：…（一句话写清要把什么东西「带回家」）

## 2. License & 合规说明

- **上游 License**：…（文件路径：`references/<repo>/LICENSE`）
- **本项目 License**：…
- **当前借鉴层级下的合规义务**：
  - [ ] …（例：在落地代码顶部加 `# Adapted from <url>, originally
        licensed under MIT, © <upstream author>` 注释）
  - [ ] …（例：保留上游 LICENSE 副本到 `third_party/<repo>/LICENSE`）
- **绝对不能做的事**：
  - …（例：不得把 GPL 代码直接 `import` 进 MIT 项目）
  - …

## 3. 核心收获

> 每条都附 `path/to/file:Lstart-Lend` 引用，禁止空话。

1. …（`src/foo.py:10-42`）
2. …（`tests/conftest.py:1-30`）
3. …

## 4. 可移植性评估

| 收获编号 | 依赖（库 / 语言特性 / 基础设施） | 工作量（S/M/L） | 风险 | 与本项目契合度（1-5） |
|----------|--------------------------------------|------------------|------|------------------------|
| 1 | … | M | 低 | 4 |
| 2 | … | S | 低 | 5 |
| 3 | … | L | 中（需要新依赖 X） | 3 |

## 5. 落地步骤（针对本项目）

> 列出本项目中将要新增 / 修改的文件，按依赖顺序排列。每条带文件路径。

- [ ] 1. 在 `<本项目路径>` 新增 …
- [ ] 2. 修改 `<本项目路径>` 的 …
- [ ] 3. 写 `tests/test_…` 覆盖新加的逻辑
- [ ] 4. （若借鉴层级 = C/D）按 §2「合规义务」加 attribution / LICENSE 副本

## 6. 不建议照搬的地方

- …（例：上游为了支持 Windows 把路径处理写得很啰嗦，本项目仅 macOS/Linux
  可以省掉）
- …（例：上游使用 GPL 许可的 `<lib>`，与本项目 MIT 不兼容，跳过）

## 7. 后续追问（向用户）

- …（例：本项目要不要也支持 X？如果要，落地步骤需扩到 §5.5）
- …

---

## 复参考增量章节

> 每次重新参考此项目时，Cursor 在此追加一节，不修改上方原内容。

### `<YYYY-MM-DD>` — 触发：「<用户原话或简述>」

- 本次新增焦点 / 问题：…
- 新发现：…（带 `path:Lstart-Lend`）
- 对原计划的影响：…
- 新增落地任务：
  - [ ] …
